import React from 'react';
import styled from 'styled-components';
import { IMenu } from '../../data';

interface ISlideProps {
  menus: IMenu[];
}

const Wrapper = styled.ul``;

const MenuSlide = ({ menus }: ISlideProps) => {
  return <Wrapper>
		
	</Wrapper>;
};

export default MenuSlide;
